---
status: DONE
---

# PR-072c: Coverage 100% for routes.py - overview, drives, maintenance, healthz

Branch: pr-072c-coverage-routes-overview
- Type: refactor
- Complexity: low
- Depends on: PR-072b
- Priority: 2
- Coder: any

## Problem

PR-072 subdivision chunk **c**: covers the first slice of `web/routes.py` - the dashboard core endpoints (health, maintenance, overview, drives list, drive detail).

Current state for routes.py: 84% coverage, 196 missed statements + 41 missed branches across 35+ endpoints. This PR (c) covers a narrow band of about 12 missed statements and 5 branches, all in the dashboard core (lines 239 through 528).

The 100% requirement is non-negotiable. `# pragma: no cover` is forbidden.

## Scope

In scope:
- Tests for `routes.py` lines 239-528 (the section containing):
  - `@router.get("/health")` (239)
  - `@router.post("/maintenance/start")` (258)
  - `@router.post("/maintenance/stop")` (287)
  - `@router.get("/maintenance")` (304)
  - `@router.get("/healthz")` (324)
  - `@router.get("/")` (341, overview)
  - `@router.get("/partials/overview")` (359)
  - `@router.get("/drives")` (372)
  - `@router.get("/drives/{enclosure_id}/{slot_id}")` (387)
  - `@router.get("/drives/{slot_ref}")` (424)
  - `@router.post("/drives/{enclosure}:{slot}/locate/start")` (437)
  - `@router.post("/drives/{enclosure}:{slot}/locate/stop")` (443)

Missed lines in this range (from `--cov-report=term-missing`):
- 312→314 (branch in /maintenance GET)
- 428 (locate? boundary in drive detail or after)
- 432-433 (error path in drive detail)

So actual coverage gap is small but ranges across these endpoints. Inspect each endpoint and write tests that exercise its 5xx/4xx error paths.

Out of scope:
- Replace/locate endpoints' deep error paths. PR-072d.
- Patrol-read endpoints. PR-072e.
- Consistency-check endpoints. PR-072f.
- Foreign-config endpoints. PR-072g.
- Charts/events/audit/partials. PR-072h.

## Files to create or extend

`tests/test_web/test_routes_overview_branches.py`:
- Test: GET /maintenance when current state is mid-expiry (branch 312→314).
- Test: error paths in drive_detail (lines 428, 432-433).
- Any other branch in the 239-528 range surfaced by `--cov-branch --cov-report=term-missing` after the other PRs land.

`tests/conftest.py`: minimal additions if needed.

## Files NOT to touch

- Production code in `web/routes.py`. No changes; only tests.
- Files outside scope.

## Implementation notes

Identify missing branches precisely by running:
```
python -m pytest --cov=src/megaraid_dashboard/web/routes --cov-branch --cov-report=term-missing
```

Then for each missed line, locate it in routes.py, understand the conditional, and write a test that exercises the missed side. Many of these are `try/except SQLAlchemyError`, `except StorcliError`, or `if not snapshot:` early-return paths. Cover each via fixture mock-raising or DB-in-known-state.

Branch coverage requires testing both sides of every `if/else`. Use `pytest.mark.parametrize` to fold related cases.

## Risk

None expected. Tests-only PR for the dashboard endpoints.

## Success criteria

1. `ruff check`, `ruff format --check`, `mypy src` pass.
2. All missed lines and branches in `web/routes.py` between line 239 and line 528 are covered (verify via `--cov-report=term-missing`).
3. No `# pragma: no cover` introduced.
4. Other files' coverage does not regress.
5. PR description includes a snippet of the term-missing output for routes.py showing the in-scope range now clean.
6. No file outside scope modified.
