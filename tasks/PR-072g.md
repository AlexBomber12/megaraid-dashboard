---
status: DONE
---

# PR-072g: Coverage 100% for routes.py - foreign-config endpoints

Branch: pr-072g-coverage-routes-foreign-config
- Type: refactor
- Complexity: medium
- Depends on: PR-072f
- Priority: 2
- Coder: any

## Problem

PR-072 subdivision chunk **g**: covers foreign-config endpoints in `web/routes.py` (lines 2454 through 2957).

3 endpoints in this range:
- `GET /controller/foreign-config` (2454) - current foreign-config metadata
- `POST /controller/foreign-config/import` (2506) - adopt foreign config
- `POST /controller/foreign-config/clear` (2631) - wipe foreign config

These are the highest-risk endpoints in the codebase (clear means irreversible loss of foreign drive metadata). They require typed-confirmation, and tests must exercise every confirmation-mismatch path.

Missed lines:
- 2467-2474 (GET error path)
- 2498-2501 (GET response shaping)
- 2514, 2517 (import body parsing)
- 2639, 2642 (clear body parsing)
- 2781-2782 (import audit + storcli error path)
- 2819-2825 (import dry-run vs commit)
- 2880-2881, 2884-2885 (import response shaping)
- 2896-2897, 2900-2901 (import edge cases)
- 2949-2955 (clear final response)

Total: ~55 missed statements, ~8 missed branches.

## Scope

In scope:
- Tests for `GET /controller/foreign-config` error paths.
- Tests for `POST /controller/foreign-config/import`:
  - Missing FCID in body → 400.
  - FCID mismatch with what storcli reports → 409.
  - Storcli error → 502.
  - Audit failure → 500.
  - Dry-run vs commit boundary.
  - Active rebuild blocks import → 409.
- Tests for `POST /controller/foreign-config/clear`:
  - Missing typed confirmation `CLEAR FOREIGN CONFIG` → 400.
  - Confirmation typo → 400.
  - Storcli error → 502.
  - Audit failure → 500.

Out of scope:
- Other routes.py sections.

## Files to create

`tests/test_web/test_routes_foreign_config_branches.py`:
- Per endpoint per error path.

## Files NOT to touch

- Production code in `web/routes.py`.

## Implementation notes

Foreign-config is the HIGHEST-RISK endpoint surface. Tests must:
1. Cover every state-machine prerequisite (no foreign config → import returns 409 with documented message).
2. Cover the typed-confirmation flow for clear (typo, missing, wrong case).
3. Cover storcli error responses (mock runner raises).
4. Cover audit failure paths (mock session raises).

Important: PR-064 (foreign config workflow) was the original task. The implementation may have placeholder storcli commands marked TBD per firmware verification. Tests should work against the mock runner regardless of the exact command strings; assert the command builder produces the expected argv.

## Risk

This covers irreversible array-state mutation endpoints. Tests must be exhaustive.

## Success criteria

1. `ruff check`, `ruff format --check`, `mypy src` pass.
2. All missed lines and branches in `web/routes.py` between line 2454 and line 2957 are covered.
3. No `# pragma: no cover` introduced.
4. Other files' coverage does not regress.
5. Risk heading in PR description due to foreign-config sensitivity.
6. PR description includes term-missing snippet showing this range clean.
7. No file outside scope modified.
