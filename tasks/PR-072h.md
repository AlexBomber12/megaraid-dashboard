---
status: DONE
---

# PR-072h: Coverage 100% for routes.py - charts, events, audit, partials

Branch: pr-072h-coverage-routes-tail
- Type: refactor
- Complexity: low
- Depends on: PR-072g
- Priority: 2
- Coder: any

## Problem

PR-072 subdivision chunk **h** (final): covers the tail of `web/routes.py` (lines 2958 through end) - charts, events page, audit log, partials.

Endpoints in this range:
- `GET /drives/{e}/{s}/charts` (2958) - per-drive temperature history chart
- `GET /events` (3001) - events page
- `GET /audit` (3041) - audit log (filtered events)
- `GET /partials/events` (3052) - HTMX events partial

Missed lines:
- 3105 (single line in charts)
- 3199, 3204 (events page filter handling)
- 3268-3269, 3281-3282 (events partial render)
- 3292-3293 (events partial empty-state)
- 3295 (single line)
- 3313→3311 (branch in events partial)
- 3421-3434 (audit log query)
- 3488, 3493, 3500 (audit log response shaping)
- 3944, 3951-3955 (final boundary; possibly module-level helpers or static-mount registration)

Total: ~30 missed statements, ~6 missed branches.

## Scope

In scope:
- Tests for `/drives/.../charts`: snapshot data shaped into chart JSON; error paths.
- Tests for `/events`: filtering by severity, category, date range; HTML response.
- Tests for `/audit`: only `operator_action` events; HTML response.
- Tests for `/partials/events`: HTMX swap; empty state; with filter.

Out of scope:
- Other routes.py sections.

## Files to create

`tests/test_web/test_routes_charts_branches.py`:
- Per branch in /drives/.../charts.

`tests/test_web/test_routes_events_branches.py`:
- /events with various filters: severity=warning, category=drive_state, date range.
- /events empty state (no events in DB).

`tests/test_web/test_routes_audit_branches.py`:
- /audit shows only operator_action; verify other categories excluded.
- /audit empty state.

`tests/test_web/test_routes_partials_events_branches.py`:
- /partials/events HTMX response.
- /partials/events empty state.

## Files NOT to touch

- Production code in `web/routes.py`.

## Implementation notes

These endpoints are mostly read-only - fewer error paths than mutating endpoints. Common cases:
1. Empty database → empty list / placeholder HTML.
2. Filter combinations → narrowed query.
3. Invalid filter values → 422 or graceful default.
4. SQLAlchemyError on read → 500 (less common for read; may not happen for these endpoints, so just verify the happy path).

The "tail" lines 3944-3955 may be module-level setup (static mount, error handlers, version emit). Investigate via code inspection; cover via integration tests against the app.

## Risk

None expected. Tests-only PR for read endpoints.

## Success criteria

1. `ruff check`, `ruff format --check`, `mypy src` pass.
2. All missed lines and branches in `web/routes.py` from line 2958 to end are covered.
3. **routes.py at 100% line + 100% branch coverage overall** (combined with PR-072c, d, e, f, g).
4. **The entire `src/megaraid_dashboard/web/` directory at 100% line + 100% branch.**
5. **Together with PR-072a + PR-072b: the entire `src/megaraid_dashboard/` at 100% line + 100% branch.**
6. No `# pragma: no cover` introduced anywhere.
7. PR description includes the FINAL `--cov-report=term-missing` output showing 100% across all files.
8. No file outside scope modified.
