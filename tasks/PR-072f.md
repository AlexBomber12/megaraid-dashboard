# PR-072f: Coverage 100% for routes.py - consistency-check endpoints

Branch: pr-072f-coverage-routes-consistency-check
- Type: refactor
- Complexity: medium
- Depends on: PR-072e
- Priority: 2
- Coder: any

## Problem

PR-072 subdivision chunk **f**: covers consistency-check endpoints in `web/routes.py` (lines 1973 through 2453).

4 endpoints in this range:
- `GET /controller/consistency-check` (1973) - current state
- `POST /controller/consistency-check/start` (1994) - trigger on VD or all
- `POST /controller/consistency-check/stop` (2054) - abort
- `POST /controller/consistency-check/mode` (2104) - auto/manual

Missed lines:
- 1979-1986 (GET error path / state derivation)
- 1999, 2007 (start validation)
- 2016-2025 (start VD selection)
- 2033 (single line in start)
- 2056-2095 (stop endpoint and its error branches)
- 2108, 2111-2112 (mode validation)
- 2130, 2132, 2135-2136 (mode body parsing)
- 2148, 2151-2152 (mode storcli error)
- 2165-2167 (mode audit error)
- 2173 (mode response normalization)
- 2223-2236 (status detail handler if separate)
- 2284-2290 (inconsistency event emission helper)
- 2307-2318 (status JSON shaping)
- 2348-2349 (response polish)
- 2391-2397 (rebuild status linking if any)
- 2449-2451 (final boundary)

Total: ~60 missed statements, ~9 missed branches. This is the largest sub-PR; if it still doesn't fit in one execution slot, can be split further into PR-072f-1 (start/stop) and PR-072f-2 (mode + status).

## Scope

In scope:
- Tests for all 4 consistency-check endpoints' missed branches.
- Tests for the inconsistency event emission helper (2284-2290) if it lives in this section.
- Tests for any rebuild-status / progress shaping (2307-2318) if in this range.

Out of scope:
- Other routes.py sections.

## Files to create

`tests/test_web/test_routes_consistency_check_branches.py`:
- Per endpoint per error path.
- Parametrize mode values (auto, manual) and per-VD ID selection.

## Files NOT to touch

- Production code in `web/routes.py`.

## Implementation notes

Same shape as PR-072e (patrol-read) but with 4 endpoints and slightly larger missed set. Tests:
1. Storcli error → 502 with body.
2. Audit failure → 500 (PR-069 contract).
3. Invalid body → 400 / 422.
4. State-machine violation (start when running, stop when not running, etc).
5. Empty VD list edge case in start (when running against "all").
6. Inconsistency event emission: snapshot has inconsistency flag, detector emits warning event; verify event row written.

Consistency-check has a unique aspect compared to patrol-read: it can target a specific VD (`/c0/v0`) or all (`/c0/vall`). Both code paths need testing.

## Risk

None expected. Tests-only PR. If too large, split into f-1 and f-2 mid-execution.

## Success criteria

1. `ruff check`, `ruff format --check`, `mypy src` pass.
2. All missed lines and branches in `web/routes.py` between line 1973 and line 2453 are covered.
3. No `# pragma: no cover` introduced.
4. Other files' coverage does not regress.
5. PR description includes term-missing snippet showing this range clean.
6. If the PR ran over time and had to split into f-1 / f-2, the description names the split and which file does what.
7. No file outside scope modified.
